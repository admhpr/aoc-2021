const input = Deno.readTextFileSync("input")
  .split("\n")
  .map((x: string) => x.split("-"))
// create map
// populate map
// part one
function solve(input: string[]) {
  console.log(input)
}
solve(input)
